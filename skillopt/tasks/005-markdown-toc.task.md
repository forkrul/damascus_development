split: val
---
We need a tool that generates a table of contents for a markdown file from
its headings, respecting heading levels, with stable anchor links, and
idempotent on re-run (updates the existing TOC block instead of appending).
