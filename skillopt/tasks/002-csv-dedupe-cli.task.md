split: train
---
Wouldn't it be cool if we had a small CLI that dedupes rows in a CSV file?
You pick which columns define a duplicate, choose keep-first or keep-last,
and it writes the cleaned file plus a short report of what got dropped.
